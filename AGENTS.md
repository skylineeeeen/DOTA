# Agent Guidance

When working on DOTA-specific setup, benchmark execution, dataset mapping, configuration, reproduction, or troubleshooting tasks in this repository, read `skills/dota/SKILL.md` first and then follow only the referenced files needed for the task.

Keep this repository-specific guidance scoped to this checkout. Do not treat `skills/dota/` as a general user-level skill unless the user explicitly installs or links it into their Codex skills directory.
